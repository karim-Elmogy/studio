# 🎛️ دليل استخدام لوحة التحكم - إضافة وتعديل المحتوى

## ✅ ما تم إنشاؤه

تم إنشاء نظام كامل لإدارة المحتوى من لوحة التحكم!

### الملفات المُنشأة:

```
resources/views/admin/services/
├── index.blade.php     ✅ (عرض جميع الخدمات)
├── create.blade.php    ✅ (إضافة خدمة جديدة)
└── edit.blade.php      ✅ (تعديل خدمة)

app/Http/Controllers/Admin/
└── ServiceController.php ✅ (جاهز بالكامل)
```

---

## 🚀 كيف تستخدم لوحة التحكم؟

### الخطوة 1: تشغيل Migrations أولاً ⚠️

```bash
php artisan migrate
```

هذا سيُنشئ جدول `services` في قاعدة البيانات.

### الخطوة 2: إضافة Routes (مهم جداً!)

افتح ملف `routes/web.php` وأضف هذا الكود داخل `Route::middleware(['auth'])->group()`:

```php
Route::middleware(['auth'])->group(function () {
    Route::get('/dashboard', function () {
        // ... الكود الموجود
    })->name('dashboard');

    // أضف هذا السطر الجديد:
    Route::resource('admin/services', \App\Http\Controllers\Admin\ServiceController::class)
        ->names('admin.services');

    // باقي الـ Routes
});
```

### الخطوة 3: افتح لوحة التحكم

```
http://127.0.0.1:8080/admin/services
```

---

## 📋 الصفحات المتاحة

### 1. عرض جميع الخدمات
**URL:** `/admin/services`
**الوصف:** عرض قائمة بجميع الخدمات مع إمكانية:
- ✅ التعديل
- ✅ الحذف
- ✅ رؤية الترتيب والحالة

### 2. إضافة خدمة جديدة
**URL:** `/admin/services/create`
**الوصف:** نموذج لإضافة خدمة جديدة

**الحقول المطلوبة:**

#### 🇬🇧 محتوى إنجليزي:
- ✅ العنوان (English Title)
- ✅ الوصف (English Description)
- ⭕ المميزات (English Features) - اختياري

#### 🇸🇦 محتوى عربي:
- ✅ العنوان (Arabic Title)
- ✅ الوصف (Arabic Description)
- ⭕ المميزات (Arabic Features) - اختياري

#### ⚙️ إعدادات:
- ✅ الترتيب (Order) - رقم (الأصغر يظهر أولاً)
- ✅ الحالة (Active) - مفعل/غير مفعل

#### 📁 صور:
- ⭕ أيقونة (Icon) - اختياري
- ⭕ صورة (Image) - اختياري

### 3. تعديل خدمة
**URL:** `/admin/services/{id}/edit`
**الوصف:** تعديل خدمة موجودة

**نفس الحقول مع:**
- ✅ عرض الصور الحالية
- ✅ إمكانية تغيير الصور
- ✅ الاحتفاظ بالصور القديمة إذا لم ترفع جديدة

### 4. حذف خدمة
**الطريقة:** من صفحة العرض، زر "Delete"
**التأكيد:** يطلب تأكيد قبل الحذف
**ماذا يحدث:**
- ✅ حذف السجل من قاعدة البيانات
- ✅ حذف الصور المرتبطة

---

## 💡 مثال عملي: إضافة خدمة "Branding"

### 1. افتح صفحة الإضافة
```
http://127.0.0.1:8080/admin/services/create
```

### 2. املأ النموذج:

**English Content:**
- Title: `Branding`
- Description: `Strong branding sets your startup apart, signaling quality and professionalism.`
- Features: (كل سطر ميزة منفصلة)
```
Brand Naming
Creative Direction
Brand Strategy
Graphic charter
Logo Design
```

**Arabic Content:**
- Title: `العلامة التجارية`
- Description: `العلامة التجارية القوية تميز شركتك الناشئة وتشير إلى الجودة والاحترافية.`
- Features:
```
تسمية العلامة التجارية
التوجيه الإبداعي
استراتيجية العلامة التجارية
الميثاق الجرافيكي
تصميم الشعار
```

**Settings:**
- Order: `1` (سيظهر أولاً)
- Active: ✅ (مفعل)

### 3. اضغط "Save Service"

### 4. النتيجة:
- ✅ سيتم حفظ الخدمة في قاعدة البيانات
- ✅ سيتم تحويلك لصفحة العرض
- ✅ ستظهر رسالة نجاح
- ✅ الخدمة ستظهر في الموقع مباشرة!

---

## 🎨 كيف تعمل المميزات (Features)؟

### في صفحة الإضافة:
اكتب كل ميزة في **سطر منفصل**:

```
Feature 1
Feature 2
Feature 3
```

### في قاعدة البيانات:
سيتم حفظها كـ JSON:
```json
[
    {"en": "Feature 1", "ar": "الميزة 1"},
    {"en": "Feature 2", "ar": "الميزة 2"},
    {"en": "Feature 3", "ar": "الميزة 3"}
]
```

### في الموقع:
سيتم عرضها كقائمة مرتبة حسب اللغة المختارة.

---

## 📁 كيف تعمل الصور؟

### رفع صورة:
1. اختر الصورة من جهازك
2. سيتم حفظها في:
   - Icon: `storage/app/public/services/icons/`
   - Image: `storage/app/public/services/images/`

### عرض الصورة في الموقع:
```blade
<img src="{{ asset('storage/' . $service->icon) }}" alt="Icon">
<img src="{{ asset('storage/' . $service->image) }}" alt="Image">
```

### مهم: تفعيل Storage Link
```bash
php artisan storage:link
```

هذا الأمر ينشئ رابط بين `storage/app/public` و `public/storage`

---

## ⚙️ الإعدادات المتقدمة

### Order (الترتيب):
- رقم أصغر = يظهر أولاً
- مثال:
  - Order: 1 → Branding (أول خدمة)
  - Order: 2 → Digital Design (ثاني خدمة)
  - Order: 10 → Development (آخر خدمة)

### Active (الحالة):
- ✅ مفعل: الخدمة تظهر في الموقع
- ❌ غير مفعل: الخدمة مخفية (لكن موجودة في قاعدة البيانات)

**استخدام:** مفيد لإخفاء خدمة مؤقتاً بدون حذفها.

---

## 🔍 الميزات المتقدمة في Controller

### 1. Validation (التحقق من البيانات)
```php
'title_en' => 'required|string|max:255',
'title_ar' => 'required|string|max:255',
'description_en' => 'required|string',
'description_ar' => 'required|string',
'icon' => 'nullable|image|mimes:jpeg,png,jpg,gif,svg|max:2048',
```

### 2. File Upload (رفع الملفات)
- تحميل تلقائي إلى `storage/app/public/services/`
- حذف الصورة القديمة عند التحديث
- حذف جميع الصور عند الحذف

### 3. Features Processing (معالجة المميزات)
- تحويل النص إلى مصفوفة
- دمج الإنجليزي والعربي
- حفظ كـ JSON في قاعدة البيانات

---

## 🎯 الخطوات التالية

### 1. أضف Routes (الآن!)
افتح `routes/web.php` وأضف:
```php
Route::resource('admin/services', \App\Http\Controllers\Admin\ServiceController::class)
    ->names('admin.services');
```

### 2. شغل Migrations (إذا لم تشغلها)
```bash
php artisan migrate
```

### 3. فعّل Storage Link
```bash
php artisan storage:link
```

### 4. جرّب إضافة خدمة
```
http://127.0.0.1:8080/admin/services/create
```

### 5. شاهد النتيجة
```
http://127.0.0.1:8080/admin/services
```

---

## 📊 الصفحات الأخرى (قريباً)

باستخدام نفس الطريقة، يمكنك إنشاء صفحات لـ:
- ✅ Projects (المشاريع)
- ✅ Blogs (المقالات)
- ✅ Testimonials (الآراء)
- ✅ FAQs (الأسئلة الشائعة)
- ✅ Contacts (الرسائل - عرض فقط)

---

## 🆘 مشاكل شائعة وحلولها

### المشكلة 1: 404 عند فتح `/admin/services`
**الحل:** تأكد من إضافة Routes في `routes/web.php`

### المشكلة 2: الصور لا تظهر
**الحل:**
```bash
php artisan storage:link
```

### المشكلة 3: خطأ "Class Service not found"
**الحل:**
1. تأكد من وجود `app/Models/Service.php`
2. شغل: `composer dump-autoload`
3. تأكد من أن Model يحتوي على الكود الكامل من `IMPLEMENTATION_GUIDE.md`

### المشكلة 4: خطأ "Table services doesn't exist"
**الحل:**
```bash
php artisan migrate
```

### المشكلة 5: Features لا تحفظ
**الحل:** تأكد من أن Model يحتوي على:
```php
protected $casts = [
    'features' => 'array',
];
```

---

## 🎉 تهانينا!

الآن لديك نظام كامل لإدارة الخدمات من لوحة التحكم!

**ما يمكنك فعله:**
- ✅ إضافة خدمات جديدة
- ✅ تعديل الخدمات الموجودة
- ✅ حذف الخدمات
- ✅ رفع صور
- ✅ ترتيب الخدمات
- ✅ تفعيل/تعطيل الخدمات
- ✅ دعم اللغتين (عربي/إنجليزي)

**الخطوة التالية:**
اتبع نفس الطريقة لإنشاء صفحات إدارة Projects, Blogs, وباقي المحتوى!

---

**بالتوفيق! 🚀**
